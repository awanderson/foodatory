<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp"
    android:paddingBottom="8dp" >
        
    <TextView
        android:textStyle="bold"
        android:id="@+id/direction_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:text="1"
        android:textSize="30sp" 
        android:layout_centerVertical="true" />
    
    <FrameLayout
        android:id="@+id/drag_handle"
        android:layout_width="32dp"
        android:layout_height="44dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/grip_background" >
        
    </FrameLayout>

    <TextView
        android:id="@+id/direction_content"
        android:layout_centerVertical="true"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/drag_handle"
        android:layout_toRightOf="@id/direction_order"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="Direction Content"
        android:textSize="16sp"
        android:textColor="@color/list_sub_item" />
    
</RelativeLayout>